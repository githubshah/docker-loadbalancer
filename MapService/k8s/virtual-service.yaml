apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: map-virtualservice
  namespace: test
spec:
  hosts:
    - map-service
  http:
    - route:
        - destination:
            host: map-service
            subset: v1
          weight: 90
        - destination:
            host: map-service
            subset: v2
          weight: 10
      # --- NEW RETRY LOGIC BELOW ---
      retries:
        attempts: 3            # Try 3 extra times
        perTryTimeout: 2s      # Give each try 2 seconds to respond
        retryOn: 5xx,gateway-error,connect-failure